# dbt lives in its own venv (built by the Dockerfile) to avoid clashing with
# Airflow's dependency pins. Install locally too if you run dbt by hand:
#   pip install -r requirements-dbt.txt
dbt-core>=1.7,<2.0
dbt-snowflake>=1.7,<2.0
