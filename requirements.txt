# Airflow image (Astro) + Extract/Load runtime dependencies.
# dbt is installed separately into an isolated venv (see requirements-dbt.txt and
# the Dockerfile) because its pins clash with Airflow's.
apache-airflow-providers-amazon
apache-airflow-providers-snowflake
boto3
snowflake-connector-python
pyyaml
python-dotenv
